version: "3.9"

services:
  snapshot:
    build: .
    container_name: creality_snapshot
    environment:
      CAM_URL: "http://192.168.3.85:8000/"
      INTERVAL: 5
    ports:
      - "5000:5000"
    restart: unless-stopped
